# Gemini / Nano Banana — `NEEDS_KEY`

**Путь доступа:** официальный SDK `google-genai==1.48.0` (установлен).
Нужен `GEMINI_API_KEY`.

Роль в pipeline: альтернативный image-канал помимо Higgsfield, кадры со сложной
геометрией и 4K. Через MCP также доступна `gemini_omni` — референс-ориентированное
видео 720p, используется для **черновых превизов** до дорогой генерации.
