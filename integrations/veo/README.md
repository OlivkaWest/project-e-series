# Veo 3 (Google) — `VIA_HIGGSFIELD`

Модель `veo3`, варианты `veo-3-preview` (качество) и `veo-3-fast` (скорость).
Только `start_image`, управления финальным кадром нет.

**Роль:** атмосферные планы — пыль в луче, свет под дверью, двор. Всегда `sound: off`.
