# veo3 — среда

Пыль в луче, дыхание света под дверью, двор, погода. Только `start_image`,
управления финальным кадром нет — поэтому в кадрах с руками и лицами не используется.

**Параметры:** `variant: "veo-3-preview"` для финала, `veo-3-fast` для проб.
