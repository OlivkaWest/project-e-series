# nano_banana_pro — сложная геометрия и 4K

Берётся там, где soul_2 ломает перспективу: длинные коридоры, панели, архитектура.
Структура промпта та же. Текст всё равно не генерируем.

Прямой путь через `google-genai` — `NEEDS_KEY`, см. `integrations/gemini/`.
