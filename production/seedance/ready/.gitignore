# Пакеты для Seedance собираются из incoming/REFERENCE_DUMP/ и отдаются владельцу
# файлами. Сами картинки и архивы в историю не кладём: оригиналы уже в дампе,
# а копии под корневым правилом уходили бы в Git LFS, который закрыт политикой
# исходящего трафика сессии.
UPLOAD/*.png
UPLOAD/*.jpg
*.zip
