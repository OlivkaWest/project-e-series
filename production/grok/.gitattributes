# Contact sheet — маленький JPEG, который должен лежать в истории и открываться
# прямо на GitHub. Выводим его из-под корневого правила Git LFS: хост lfs.github.com
# закрыт политикой исходящего трафика сессии, файлы в LFS отсюда не отправляются.
*.jpg -filter -diff -merge -text
