# Ассеты EP01

## Эталонные ассеты (прогон 21.08.2026)

| Файл | Что | Вердикт |
|---|---|---|
| `ref-corridor-A.png` | коридор, вариант A | палитра верная, но в конце открытый проём вместо закрытой двери |
| `ref-corridor-B.png` | коридор, вариант B | **лучший из двух**: закрытая дверь, свет только снизу |
| `ref-door46-A.png` | дверь 46, вариант A | **лучший из двух**: белая дверь, номер мелкий, полоса снизу |
| `ref-door46-B.png` | дверь 46, вариант B | свет по всему периметру — портал, отклонён |
| `ref-handle-A.png` | ручка, вариант A | **не дошёл до репозитория** |
| `ref-handle-B.png` | ручка, вариант B | на ручке висит ключ, хотя требовалась пустая — отклонён |
| `ref-panel-A.png` | панель лифта, вариант A | фактура эталонная, цифры в случайном порядке |
| `ref-panel-B.png` | панель лифта, вариант B | не сравнивался: A уже проходит по фактуре |

Разбор целиком — в `../PROMPTS.md`, раздел «Журнал попыток».

## Почему файлов может не быть в репозитории

Git LFS настроен (`.gitattributes`), но хост `lfs.github.com` закрыт политикой
исходящего трафика удалённой сессии — запушить бинарники оттуда нельзя.
Политика не обходится.

**Файлы кладёт владелец со своей машины:**

```bash
git lfs install
git add season-01/episode-001/assets/*.png
git commit -m "assets: эталонные кадры EP01"
git push
```

## Принятые эталоны

| Ассет | Модель | Job ID | Дата |
|---|---|---|---|
| ручка двери 46 | `nano_banana_pro` | `e79996c1-5d64-48cc-a059-2659e9a57cd2` | 21.08.2026 |

Все кадры EP01, где есть ручка, наследуют этот эталон. Дверь и коридор — в работе,
журнал прогонов в `../PROMPTS.md`, раздел 5.

## Именование

```
ref-<ассет>-<вариант>.png     эталонные ассеты локации
ep001-sh<NN>.png              принятый мастер-кадр шота
ep001-v<NN>.wav               реплика
sb-sh<NN>.png                 раскадровка
```
