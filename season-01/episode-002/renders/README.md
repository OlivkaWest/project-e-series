# Рендеры эпизода

Сырые генерации, до монтажа.

Именование: `ep002-<shot>-v<NN>.<ext>` — например `ep001-sh02-v03.mp4`.
Версия увеличивается на каждую попытку, старые не удаляем до `final`.
Выбранный дубль отмечается в `SHOTLIST.md` статусом `approved`.

Файлы в git не коммитим.
