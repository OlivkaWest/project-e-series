# В корне *.mp4/*.mov/*.wav/*.aiff не коммитятся вовсе.
# Для этой папки правило снимается: сюда владелец грузит в том числе
# видео- и аудиореференсы, и они должны доезжать до разбора.
!*.mp4
!*.mov
!*.wav
!*.aiff
!*.mp3
